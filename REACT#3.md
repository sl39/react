### REACT#3
```javascript
ReactDOM.render(<App />, root)
// root tag안에 App을 넣어서 랜더링 한다
const [minutes, setMinutes] = React.useState(a)
// React.useState(a) 의 변수를 minutes 와 setMinutes에 각각 할당하는데
//  minutes 의 값은 a로 할당이 되고
//  setMinute는 함수로 써, minutes가 변환이 되면 그거에 맞게 변환을 시킨후
// 랜더링 시킨다
```